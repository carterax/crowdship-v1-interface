import { darken, mode, whiten } from '@chakra-ui/theme-tools';

export const progressStyles = {
  baseStyle: {
    filledTrack: {
      bgColor: 'green.300',
    },
    track: {
      bg: 'rgba(0, 0, 0, 6%)',
    },
  },
};
