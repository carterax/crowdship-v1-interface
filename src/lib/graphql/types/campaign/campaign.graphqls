type Campaign {
  id: ID!
  name: String!
  description: String
  category: String!
  slug: String!
  previewImage: String
}

type Query {
  campaigns: Campaign!
}

type Mutation {
  createCampaign(
    name: String!
    description: String
    previewImage: String
  ): Campaign!
}
