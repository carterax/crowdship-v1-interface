mutation CreateCampaign(
  $name: String!
  $description: String
  $previewImage: String
) {
  createCampaign(
    name: $name
    description: $description
    previewImage: $previewImage
  ) {
    id
    name
    description
    category
    slug
    previewImage
  }
}
